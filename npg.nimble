# Package

version       = "0.1.0"
author        = "Max Skybin"
description   = "Password generator in Nim"
license       = "MIT"
srcDir        = "src"
bin           = @["npg"]

# Dependencies

requires "nim >= 1.0.0"
requires "passgen >= 0.1.0"